{
  "name": "discord-bot-new",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "pm2 start --no-daemon",
    "start-front": "npm run build --prefix front && npm run start --prefix front",
    "start-bot": "npm run start --prefix bot",
    "postinstall": "subpkg install",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "subpackage": "^1.1.0"
  },
  "subPackages": [
    "bot",
    "front"
]
}
